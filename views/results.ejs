<!DOCTYPE html>
<html lang="en">
  <head>
    <title>View Results: <%= title %></title>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
    <link rel="stylesheet" href="/vendor/border-box.css" />
    <link rel="stylesheet" href="/styles/layout.css" />
    <link rel="stylesheet" href="/styles/results.css" />
    <link rel="stylesheet" href="/styles/header.css" />
  </head>

  <body>
    <%- include('partials/_header') %>
    <main class="results">
      <h1><%= title %></h1>
      <h2><%= question %></h2>
      <!-- display ranking when there is a least 1 submission  -->
      <% if (choices.length > 0) {%>
      <ol>
        <% for (const choice of choices){ %>
        <!-- when there is a description for the choice, make it as the title attr -->
        <% if(choice.description) { %>
        <li title="<%= choice.description %>" %>><%= choice.title %></li>
        <% }else{ %>
        <li><%= choice.title %></li>
        <% }} %>
      </ol>
      <% } else { %>
      <!-- when there is no submission for the poll  -->
      <p>No one has voted yet.</p>
      <% }%>
    </main>
  </body>
</html>
